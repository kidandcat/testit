# Test screenshot command behavior

test "Screenshot behavior verification"
  navigate "https://example.com"
  wait_for "h1"
  
  # First screenshot - should save with test name if no file exists
  screenshot
  
  # Second screenshot - should compare and fail if different
  screenshot