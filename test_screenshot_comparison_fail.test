# Test screenshot comparison failure

test "Screenshot comparison should fail on change"
  # First, take a screenshot of example.com
  navigate "https://example.com"
  wait_for "h1"
  screenshot
  
  # Now navigate to a different page that will cause visual difference
  navigate "https://httpbin.org"
  
  # This screenshot should fail comparison if the tool compares
  screenshot